# Static inventory for the Territory Grounder deploy target.
# AWX uses its own "Territory Grounder" inventory object (host nltg01, one_key SSH credential);
# this file is for documentation and non-AWX (local `ansible-playbook -i`) runs.
[territory_grounder]
nltg01 ansible_host=10.0.X.X ansible_user=root
