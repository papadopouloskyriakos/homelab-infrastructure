apiVersion: v1
kind: Service
metadata:
  name: bentopdf
  namespace: bentopdf
  labels:
    app.kubernetes.io/name: bentopdf
    environment: production
    managed-by: argocd
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: bentopdf
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
