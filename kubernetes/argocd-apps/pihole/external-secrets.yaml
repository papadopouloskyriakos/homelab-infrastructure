***REMOVED***
# Pi-hole Credentials - ExternalSecret
***REMOVED***
# Replaces hardcoded secret.yaml - credentials fetched from OpenBao
***REMOVED***
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: pihole-credentials
  namespace: pihole
  labels:
    app.kubernetes.io/name: pihole
    environment: production
    managed-by: argocd
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao
    kind: ClusterSecretStore
  target:
    name: pihole-credentials
    creationPolicy: Owner
    deletionPolicy: Retain
  data:
    - secretKey: WEBPASSWORD
      remoteRef:
        key: k8s/pihole/credentials
        property: WEBPASSWORD
